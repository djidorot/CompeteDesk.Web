@model CompeteDesk.Models.WarIntel
@{
    ViewData["Title"] = "New Intel";
    ViewData["LayoutFluid"] = true;
    ViewData["UseSidebar"] = true;
    ViewBag.Tab = "intel";
}

<link rel="stylesheet" href="~/css/warroom.css" asp-append-version="true" />

<div class="card shadow-sm cd-warform">
    <div class="card-body">
        <h1 class="cd-h1 mb-1">New Intel</h1>
        <div class="cd-sub mb-3">Capture observations and signals. Keep it short and specific.</div>

        <form asp-action="IntelCreate" method="post">
            @Html.AntiForgeryToken()

            <partial name="_IntelForm" model="Model" />

            <div class="d-flex gap-2 mt-3">
                <button class="btn btn-primary cd-btn" type="submit">Save</button>
                <a class="btn btn-outline-secondary cd-btn" asp-action="Index" asp-route-tab="intel">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/warroom.js" asp-append-version="true"></script>
}
